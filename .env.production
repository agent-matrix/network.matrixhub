# =============================================================================
# Network MatrixHub - Production Environment Configuration
# =============================================================================
# ⚠️  IMPORTANT: Never commit this file with real secrets to version control!
# For production deployment, set these values in your hosting platform's
# environment variable configuration (e.g., Render, Vercel, AWS, etc.)
# =============================================================================

# ---------------------------------------------------------------------------
# Application Settings
# ---------------------------------------------------------------------------
APP_NAME="Network MatrixHub IO Backend"
APP_ENV=production
APP_DEBUG=false

# ---------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# ---------------------------------------------------------------------------
# Format: postgresql://username:password@host:port/database
# Example for Render: postgresql://user:pass@dpg-xxxxx.oregon-postgres.render.com/dbname
DATABASE_URL=postgresql://matrixhub:CHANGE_ME@localhost:5432/network_matrixhub

# For Render.com, this will be automatically set from the database connection
# DATABASE_URL=${DATABASE_URL}  # Render provides this automatically

# ---------------------------------------------------------------------------
# CORS Configuration
# ---------------------------------------------------------------------------
# Comma-separated list or JSON array of allowed origins
BACKEND_CORS_ORIGINS=["https://network.matrixhub.io","https://www.network.matrixhub.io","http://localhost:3000"]

# ---------------------------------------------------------------------------
# Server Configuration
# ---------------------------------------------------------------------------
PORT=8000
WORKERS=4  # Number of Uvicorn workers (recommended: 2-4 * CPU cores)
BACKEND_PORT=8000

# ---------------------------------------------------------------------------
# Security & Authentication
# ---------------------------------------------------------------------------
# Generate a strong secret key for JWT tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY_IN_PRODUCTION
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# ---------------------------------------------------------------------------
# Frontend Configuration
# ---------------------------------------------------------------------------
NEXT_PUBLIC_API_BASE_URL=https://your-backend.onrender.com
FRONTEND_PORT=3000

# ---------------------------------------------------------------------------
# Database Configuration (for docker-compose)
# ---------------------------------------------------------------------------
POSTGRES_USER=matrixhub
POSTGRES_PASSWORD=CHANGE_ME_IN_PRODUCTION
POSTGRES_DB=network_matrixhub
POSTGRES_PORT=5432

# ---------------------------------------------------------------------------
# Monitoring & Logging (Optional)
# ---------------------------------------------------------------------------
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx
# LOG_LEVEL=INFO

# ---------------------------------------------------------------------------
# Feature Flags (Optional)
# ---------------------------------------------------------------------------
# ENABLE_REGISTRATION=true
# ENABLE_GUEST_MODE=true
# RATE_LIMIT_ENABLED=true
