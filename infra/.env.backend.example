# Backend env for network.matrixhub.io

APP_NAME="Network MatrixHub IO Backend"
APP_ENV="dev"
APP_DEBUG=true

# MatrixHub DB connection (read-only recommended)
DATABASE_URL="postgresql+psycopg2://matrixhub:password@localhost:5432/matrixhub"

# CORS / API
BACKEND_CORS_ORIGINS="[\"http://localhost:3000\"]"

